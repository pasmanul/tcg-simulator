# Codex 作業ガイド

## 基本方針

- 原則として、ユーザーへの説明・質問・進捗報告は日本語で行う。
- コード、識別子、コマンド、エラーメッセージ、外部仕様名は必要に応じて英語のまま扱う。
- 作業前に関連ファイルを読み、既存の設計・命名・実装パターンに合わせる。
- 既存の未コミット変更はユーザーの作業として扱い、明示依頼なしに戻さない。
- 変更は依頼範囲に絞り、不要な大規模リファクタやメタデータ更新を避ける。
- 実装後は可能な範囲でテスト、型チェック、ビルドなどの検証を行う。

## プロジェクト情報

このリポジトリの構成、設計、実装ルール、開発コマンドは
`docs/project-guide.md` を参照する。

作業でデスクトップ版または Web 版の仕様に触れる場合は、先に
`docs/project-guide.md` の該当セクションを確認する。

## Kiro Steering

`.kiro/steering/soul.md` は Kiro 専用の steering として、そのまま残す。
ここへ重複コピーする必要はない。Kiro steering を明示的に読み込むツールで作業する場合は、
より優先度の高い system / developer 指示に従ったうえで適用する。

